<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hy.iom.mapper.oracle.InfoReflexMapper">
    <select id="selectInfoByProjectId" resultType="com.hy.iom.entities.InfoReflex">
        SELECT id, projectid, name, state
        FROM T_IOM_INFOREFLEX
        WHERE PROJECTID = #{projectId}
    </select>

</mapper>